# Verified local environment

The release package was checked with the following locally installed environment:

- Python 3.12.10
- PyTorch 2.11.0+cu128
- NumPy 2.5.0
- SciPy 1.18.0

PyTorch installation commands depend on the operating system, accelerator, and CUDA version. Users should follow the official PyTorch installation selector when the default package index does not provide the required accelerator build.
